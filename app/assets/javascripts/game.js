// Generated by CoffeeScript 1.9.1
var parseId;

$(function() {
  $('.container-slot').mouseover(function() {
    var col, colSelector;
    col = parseId($(this).attr('id'))[0];
    colSelector = '.col-' + col;
    $(colSelector).addClass('container-slot-focus');
  });
  return $('.container-slot').mouseleave(function() {
    var col, colSelector;
    col = parseId($(this).attr('id'))[0];
    colSelector = '.col-' + col;
    $(colSelector).removeClass('container-slot-focus');
  });
});

parseId = function(idStr) {
  var id, parsedIdStr;
  parsedIdStr = idStr.split('-');
  id = parsedIdStr.pop();
  return [id, parsedIdStr];
};
